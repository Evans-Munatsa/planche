<div class="media-body">
    <% @post.comments.each do |comment| %>
    <i class="fa fa-comments" aria-hidden="true">comment by: <%= @post.user.username %></i>
    <p> <%= comment.comment %></p>
    <% end %>
</div>